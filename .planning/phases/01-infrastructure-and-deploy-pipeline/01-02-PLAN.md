---
phase: 01-infrastructure-and-deploy-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .git/config
autonomous: false
requirements:
  - INFRA-02
  - INFRA-05
  - INFRA-07

user_setup:
  - service: github
    why: "Repository must exist on GitHub for CI/CD deploy"
    account_setup:
      - task: "Ensure GitHub repository evgenijbg-netizen/WallReinf-wiki exists"
        location: "https://github.com/new or verify https://github.com/evgenijbg-netizen/WallReinf-wiki"
    dashboard_config:
      - task: "After first successful CI run, verify GitHub Pages source is set to gh-pages branch / root"
        location: "Repository Settings > Pages"

must_haves:
  truths:
    - "Code is pushed to GitHub remote on main branch"
    - "GitHub Actions workflow runs and completes successfully"
    - "gh-pages branch is created by the CI workflow"
    - "Live site at https://evgenijbg-netizen.github.io/WallReinf-wiki/ loads with Material theme CSS"
    - "Czech search returns results on the live site"
    - "Light/dark mode toggle works on the live site"
  artifacts:
    - path: ".git/config"
      provides: "Git remote pointing to GitHub"
      contains: "evgenijbg-netizen/WallReinf-wiki"
  key_links:
    - from: "git push origin main"
      to: ".github/workflows/ci.yml"
      via: "GitHub Actions trigger on push to main"
      pattern: "branches: \\[main\\]"
    - from: ".github/workflows/ci.yml"
      to: "gh-pages branch"
      via: "mkdocs gh-deploy --force"
      pattern: "mkdocs gh-deploy"
---

<objective>
Initialize git repository, push all files to GitHub, and verify the CI/CD pipeline deploys the site to GitHub Pages successfully.

Purpose: Prove the entire deploy pipeline works end-to-end -- from push to live site -- before any content investment. This is the highest-leverage validation in Phase 1.
Output: A live site at https://evgenijbg-netizen.github.io/WallReinf-wiki/ with working CSS, Czech search, and light/dark toggle.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-deploy-pipeline/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-deploy-pipeline/01-RESEARCH.md
@.planning/phases/01-infrastructure-and-deploy-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git repo and push to GitHub</name>
  <files>.git/config</files>
  <read_first>
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-CONTEXT.md
    .gitignore
  </read_first>
  <action>
Initialize the git repository (if not already initialized) and push all project files to GitHub.

Steps:
1. Check if `.git` directory exists. If not, run `git init`.
2. Add the remote: `git remote add origin https://github.com/evgenijbg-netizen/WallReinf-wiki.git`
   - If remote already exists, verify it points to the correct URL with `git remote get-url origin`
3. Stage all files EXCEPT the `.planning/` directory (planning files are internal):
   ```bash
   git add mkdocs.yml requirements.txt .gitignore .github/workflows/ci.yml docs/index.md docs/assets/logo.png
   ```
   Also add WallReinforcementWizard_logo.png (the original logo in repo root) if it should be preserved.
4. Create initial commit: `git commit -m "feat: initial MkDocs Material site with CI/CD pipeline"`
5. Ensure the branch is named `main`: `git branch -M main`
6. Push: `git push -u origin main`

If push fails with authentication error, create a checkpoint for the user to authenticate. If the remote repository does not exist (404), inform the user they need to create it at https://github.com/new first.

IMPORTANT: Verify `.gitignore` is working -- `site/` directory (if it exists from local build) must NOT be staged.
  </action>
  <verify>
    <automated>git remote get-url origin | grep "evgenijbg-netizen/WallReinf-wiki" && git log --oneline -1 | grep "initial MkDocs"</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` outputs a URL containing `evgenijbg-netizen/WallReinf-wiki`
    - `git log --oneline -1` shows a commit message containing "initial MkDocs"
    - `git status` shows no untracked site/ files (gitignore working)
    - `git branch --show-current` outputs `main`
  </acceptance_criteria>
  <done>Git repository initialized with remote pointing to evgenijbg-netizen/WallReinf-wiki, initial commit pushed to main branch, site/ excluded from tracking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live site deployment on GitHub Pages</name>
  <files>none (verification only)</files>
  <read_first>
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-RESEARCH.md
  </read_first>
  <action>
Before presenting the checkpoint to the user, run `gh run list --repo evgenijbg-netizen/WallReinf-wiki --limit 1` (if gh CLI available) to check if the workflow has completed. Wait up to 2 minutes for it to finish. Then present the checkpoint verification steps to the user.

What was built: Complete MkDocs Material site with CI/CD pipeline deployed to GitHub Pages. The push to main triggered the GitHub Actions workflow, which builds the site and deploys to the gh-pages branch.

How to verify:
1. Open https://github.com/evgenijbg-netizen/WallReinf-wiki/actions -- verify the "Deploy docs" workflow ran and shows a green checkmark
2. Open https://github.com/evgenijbg-netizen/WallReinf-wiki/branches -- verify a `gh-pages` branch exists
3. If GitHub Pages is not yet configured: go to Repository Settings > Pages > set Source to "Deploy from a branch" > select `gh-pages` branch / `/ (root)` > Save
4. Visit https://evgenijbg-netizen.github.io/WallReinf-wiki/ -- the page should load with:
   - Material theme styling (blue header bar, clean typography)
   - The WRW logo in the header (dark blue concrete block with "RW")
   - Title "Wall Reinforcement Wizard" on the page
   - Czech text content
5. Click the light/dark mode toggle icon in the header (sun/moon icon) -- the theme should switch between light and dark
6. Type "vyztuz" in the search bar -- at least one result should appear
7. Verify `site/` is NOT committed: check that https://github.com/evgenijbg-netizen/WallReinf-wiki does not have a `site/` directory in the main branch file listing
  </action>
  <verify>
    <automated>gh run list --repo evgenijbg-netizen/WallReinf-wiki --limit 1 --json status --jq ".[0].status" 2>/dev/null || echo "Check GitHub Actions manually"</automated>
  </verify>
  <acceptance_criteria>
    - GitHub Actions "Deploy docs" workflow shows green checkmark
    - gh-pages branch exists in the repository
    - Site loads at https://evgenijbg-netizen.github.io/WallReinf-wiki/ with Material CSS (not raw HTML or 404)
    - Light/dark toggle switches the theme
    - Search for "vyztuz" returns at least one result
    - site/ directory is not present in the main branch on GitHub
  </acceptance_criteria>
  <done>Live site is accessible at https://evgenijbg-netizen.github.io/WallReinf-wiki/ with Material theme CSS, working Czech search, and functional light/dark mode toggle. Phase 1 infrastructure is complete.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Git remote is configured to evgenijbg-netizen/WallReinf-wiki
2. Main branch has been pushed with all infrastructure files
3. GitHub Actions workflow completed successfully (green)
4. gh-pages branch exists
5. Live site at https://evgenijbg-netizen.github.io/WallReinf-wiki/ is accessible
6. Site has Material theme CSS, Czech UI, working search, working light/dark toggle
7. site/ is not in the repository
</verification>

<success_criteria>
- Live site accessible at https://evgenijbg-netizen.github.io/WallReinf-wiki/
- Material theme renders with CSS intact
- Czech search returns results for "vyztuz"
- Light/dark mode toggle works
- GitHub Actions workflow is green
- gh-pages branch exists
- site/ not committed to main
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-deploy-pipeline/01-02-SUMMARY.md`
</output>
