---
phase: 01-infrastructure-and-deploy-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - requirements.txt
  - .gitignore
  - .github/workflows/ci.yml
  - docs/index.md
  - docs/assets/logo.png
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "mkdocs build --strict passes with zero warnings"
    - "mkdocs serve renders the index page with Material theme, Czech UI, and light/dark toggle"
    - "All dependencies are pinned to exact versions in requirements.txt"
    - "GitHub Actions workflow is configured to deploy on push to main with write permissions"
    - "site/ directory is excluded from version control"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Site configuration with Material theme, Czech search, light/dark toggle, correct site_url"
      contains: "site_url: https://evgenijbg-netizen.github.io/WallReinf-wiki/"
    - path: "requirements.txt"
      provides: "Pinned Python dependencies"
      contains: "mkdocs==1.6.1"
    - path: ".github/workflows/ci.yml"
      provides: "CI/CD pipeline for GitHub Pages deploy"
      contains: "contents: write"
    - path: ".gitignore"
      provides: "Git ignore rules excluding build output"
      contains: "site/"
    - path: "docs/index.md"
      provides: "Homepage stub in Czech"
      min_lines: 10
    - path: "docs/assets/logo.png"
      provides: "Site logo copied from repo root"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "requirements.txt"
      via: "pip install -r requirements.txt"
      pattern: "pip install -r requirements.txt"
    - from: "mkdocs.yml"
      to: "docs/assets/logo.png"
      via: "logo config path"
      pattern: "logo: assets/logo.png"
    - from: "mkdocs.yml"
      to: "docs/index.md"
      via: "nav entry"
      pattern: "index.md"
---

<objective>
Create all MkDocs Material infrastructure files for the WallReinf wiki -- configuration, dependencies, CI/CD workflow, gitignore, index page, and logo -- so that `mkdocs build --strict` passes locally and the project is ready for deployment.

Purpose: Establish the complete file foundation for a Czech-language MkDocs Material site before any git push. Every INFRA requirement (01-07) is addressed by the files created here.
Output: Six files in the project root and docs/ directory that together form a buildable MkDocs site.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-deploy-pipeline/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-deploy-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MkDocs configuration, dependencies, CI workflow, and gitignore</name>
  <files>mkdocs.yml, requirements.txt, .gitignore, .github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-RESEARCH.md
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-CONTEXT.md
  </read_first>
  <action>
Create four files with exact content specified below.

**mkdocs.yml** — Master site configuration. Use the complete example from RESEARCH.md Code Examples section with these exact values:
- `site_name: Wall Reinforcement Wizard`
- `site_url: https://evgenijbg-netizen.github.io/WallReinf-wiki/` (trailing slash required)
- `theme.name: material`
- `theme.language: cs`
- `theme.logo: assets/logo.png`
- Two `palette` entries: `scheme: default` with toggle icon `material/brightness-7` and name `Přepnout na tmavý režim`, and `scheme: slate` with toggle icon `material/brightness-4` and name `Přepnout na světlý režim`. Both with `primary: blue` and `accent: blue`.
- `features:` list: `navigation.tabs`, `navigation.sections`, `navigation.top`, `search.highlight`, `content.code.copy`
- `plugins:` with `search` (lang: cs) and `glightbox`
- `markdown_extensions:` with `admonition`, `pymdownx.details`, `pymdownx.superfences`, `pymdownx.tabbed` (alternate_style: true), `attr_list`, `md_in_html`
- `nav:` with single entry `- Domů: index.md`

**requirements.txt** — Exact pinned versions:
```
mkdocs==1.6.1
mkdocs-material==9.7.6
mkdocs-glightbox==0.5.2
```

**.gitignore** — Include these entries:
```
site/
__pycache__/
*.pyc
.venv/
venv/
.env
```

**.github/workflows/ci.yml** — GitHub Actions deploy workflow:
```yaml
name: Deploy docs

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.x
          cache: pip
      - run: pip install -r requirements.txt
      - run: mkdocs gh-deploy --force
```

CRITICAL: `permissions: contents: write` must be at the workflow level (not inside jobs). Without it, deploy silently fails (green CI but no gh-pages branch).
  </action>
  <verify>
    <automated>grep "mkdocs==1.6.1" requirements.txt && grep "mkdocs-material==9.7.6" requirements.txt && grep "mkdocs-glightbox==0.5.2" requirements.txt && grep "site_url: https://evgenijbg-netizen.github.io/WallReinf-wiki/" mkdocs.yml && grep "language: cs" mkdocs.yml && grep "lang: cs" mkdocs.yml && grep "toggle" mkdocs.yml && grep "^site/" .gitignore && grep "contents: write" .github/workflows/ci.yml && grep "branches: \[main\]" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "mkdocs==1.6.1" requirements.txt` returns a match
    - `grep "mkdocs-material==9.7.6" requirements.txt` returns a match
    - `grep "mkdocs-glightbox==0.5.2" requirements.txt` returns a match
    - `grep "site_url: https://evgenijbg-netizen.github.io/WallReinf-wiki/" mkdocs.yml` returns a match
    - `grep "language: cs" mkdocs.yml` returns a match (theme language)
    - `grep "lang: cs" mkdocs.yml` returns a match (search language — separate from theme.language)
    - `grep -c "toggle" mkdocs.yml` returns 2 or more (light and dark toggle blocks)
    - `grep -c "scheme:" mkdocs.yml` returns 2 (default + slate)
    - `grep "^site/" .gitignore` returns a match
    - `grep "contents: write" .github/workflows/ci.yml` returns a match
    - `grep "branches: \[main\]" .github/workflows/ci.yml` returns a match
    - `grep "pip install -r requirements.txt" .github/workflows/ci.yml` returns a match
    - `grep "mkdocs gh-deploy --force" .github/workflows/ci.yml` returns a match
  </acceptance_criteria>
  <done>All four config files exist with correct content. requirements.txt has three pinned deps, mkdocs.yml has Czech search + theme + light/dark toggle + correct site_url, .gitignore excludes site/, CI workflow deploys on push to main with write permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs directory with index page and logo, verify build</name>
  <files>docs/index.md, docs/assets/logo.png</files>
  <read_first>
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-CONTEXT.md
    .planning/phases/01-infrastructure-and-deploy-pipeline/01-RESEARCH.md
    mkdocs.yml
  </read_first>
  <action>
Create the docs directory structure and content:

**docs/assets/logo.png** — Copy from repo root:
```bash
mkdir -p docs/assets
cp WallReinforcementWizard_logo.png docs/assets/logo.png
```

**docs/index.md** — Czech homepage stub. IMPORTANT: Do NOT include links to pages that do not exist (getting-started/, guide/, reference/, faq.md) because `mkdocs build --strict` will fail on broken internal links. Use plain text descriptions instead.

Content for docs/index.md:
```markdown
# Wall Reinforcement Wizard

**Wall Reinforcement Wizard** je plugin pro Tekla Structures, který automatizuje
generování výztuže stěn podle zadaných parametrů.

## Pro koho je tento manuál

Pro inženýry a projektanty, kteří používají Tekla Structures a chtějí rychle
a správně konfigurovat výztuž stěn pomocí pluginu Wall Reinforcement Wizard.

## Co zde najdete

- **Začínáme** — instalace, požadavky a první spuštění pluginu
- **Průvodce funkcemi** — nastavení parametrů, otvory, třmínky, preview a validace
- **Reference** — kompletní přehled všech parametrů pluginu
- **FAQ** — časté dotazy a řešení problémů
```

Note: The section links above are intentionally plain text (bold, not hyperlinks) to avoid broken link errors in strict mode. Phase 2 will add the actual nav structure and convert these to links.

After creating both files, install dependencies locally and run the build:
```bash
pip install -r requirements.txt
mkdocs build --strict
```

The build must complete with exit code 0 and zero warnings.
  </action>
  <verify>
    <automated>test -f docs/index.md && test -f docs/assets/logo.png && pip install -r requirements.txt && mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/index.md` exits with code 0
    - `test -f docs/assets/logo.png` exits with code 0
    - `docs/assets/logo.png` file size is greater than 0 bytes (valid image copy)
    - `grep "Wall Reinforcement Wizard" docs/index.md` returns a match
    - `grep "Tekla Structures" docs/index.md` returns a match
    - `grep -c "\[.*\](.*\.md)" docs/index.md` returns 0 (no internal markdown links that could break strict build)
    - `mkdocs build --strict` exits with code 0 and produces `site/index.html`
    - `test -f site/index.html` exits with code 0 after build
  </acceptance_criteria>
  <done>docs/index.md contains Czech homepage stub without broken links, docs/assets/logo.png is copied from repo root, `mkdocs build --strict` passes with zero warnings, site/index.html is generated.</done>
</task>

</tasks>

<verification>
After both tasks complete, the following must all pass:

1. `grep "mkdocs==1.6.1" requirements.txt` — pinned MkDocs version (INFRA-01)
2. `grep "mkdocs-material==9.7.6" requirements.txt` — pinned Material version (INFRA-01)
3. `grep "contents: write" .github/workflows/ci.yml` — deploy permissions (INFRA-07)
4. `grep "branches: \[main\]" .github/workflows/ci.yml` — CI trigger (INFRA-02)
5. `grep "lang: cs" mkdocs.yml` — Czech search support (INFRA-03)
6. `grep "toggle" mkdocs.yml` — light/dark mode (INFRA-04)
7. `grep "evgenijbg-netizen.github.io/WallReinf-wiki" mkdocs.yml` — correct site_url (INFRA-05)
8. `grep "^site/" .gitignore` — site/ excluded (INFRA-06)
9. `mkdocs build --strict` — exits 0, zero warnings (ALL)
</verification>

<success_criteria>
- All six files exist: mkdocs.yml, requirements.txt, .gitignore, .github/workflows/ci.yml, docs/index.md, docs/assets/logo.png
- `mkdocs build --strict` passes with zero warnings
- Every INFRA requirement (01-07) is verifiable via grep on the created files
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-deploy-pipeline/01-01-SUMMARY.md`
</output>
