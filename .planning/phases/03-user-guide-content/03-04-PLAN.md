---
phase: 03-user-guide-content
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - docs/pruvodce/preview.md
  - docs/pruvodce/validace.md
  - docs/pruvodce/pdf-export.md
autonomous: true
requirements: [GUIDE-05, GUIDE-06, GUIDE-07, UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "User can read preview.md and understand both inline dock and standalone preview modes, including layer controls and heatmap"
    - "User can read validace.md and understand reality mode, layer legend, and how validation differs from preview"
    - "User can read pdf-export.md and follow steps to export PDF, PNG, or print from validation window"
    - "preview.md explains heatmap matrix (per CONTEXT.md: heatmapa goes on this page)"
    - "validace.md explains that it shows already-generated rebar from Tekla (not planned preview)"
  artifacts:
    - path: "docs/pruvodce/preview.md"
      provides: "Preview panel guide covering inline dock, standalone window, layers, heatmap, length adjustments"
      contains: "Verze pluginu"
      min_lines: 80
    - path: "docs/pruvodce/validace.md"
      provides: "Validation window guide covering reality mode, layers, legend"
      contains: "Verze pluginu"
      min_lines: 60
    - path: "docs/pruvodce/pdf-export.md"
      provides: "PDF/PNG/Print export guide from validation window"
      contains: "Verze pluginu"
      min_lines: 40
  key_links:
    - from: "docs/pruvodce/preview.md"
      to: "docs/pruvodce/parametry.md"
      via: "cross-link for parameter context"
      pattern: "parametry.md"
    - from: "docs/pruvodce/validace.md"
      to: "docs/pruvodce/preview.md"
      via: "cross-link distinguishing validation from preview"
      pattern: "preview.md"
    - from: "docs/pruvodce/pdf-export.md"
      to: "docs/pruvodce/validace.md"
      via: "cross-link to validation window (export lives there)"
      pattern: "validace.md"
---

<objective>
Write the three output/visualization guide pages: preview.md (preview panel with heatmap), validace.md (validation window with reality mode), and pdf-export.md (export from validation). These complete the guide section.

Purpose: These pages cover what happens after parameters are set — previewing, validating, and exporting results. They form the final third of the user journey.
Output: 3 filled guide pages completing the entire pruvodce/ section content.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-user-guide-content/03-CONTEXT.md
@.planning/phases/03-user-guide-content/03-RESEARCH.md
@.planning/phases/03-user-guide-content/03-02-SUMMARY.md
</context>

<interfaces>
<!-- From RebarPreviewWindow.xaml — key UI labels for preview.md -->
Standalone preview window labels:
- Window title: "Preview vyztue"
- Layers panel: "Vrstvy vyztuze"
- Show all: "Vse" / Hide all: "Nic"
- Length edit: "Uprava delky (vybrane rady):"
- Statistics: "Celkem: 0 prutu, 0 delek, 0 varovani"
- Collisions: "Zkontrolovat kolize"
- Normalize: "Sjednotit vybrane"
- Generate: "Generovat" / Cancel: "Zrusit"
- Heatmap: "Heatmapa (O x delka)"

<!-- From RebarValidationWindow.xaml — key UI labels for validace.md and pdf-export.md -->
Validation window labels:
- Title: "Graficka validace vyztuze"
- Reality mode: "Skutecna vyztuz z Tekla modelu"
- Refresh: "Obnovit"
- Export: "Export PDF", "Export PNG", "Tisk"
- Layers: "Vrstvy vyztuze"
- Legend entries: Svisla (Class 3), Vodorovna (Class 6), Lemovani hrany (Class 7,8), Lemovani otvoru (Class 9), Prapory (Class 9 - Flag), Trminky (Class 11)
- Transparency: "Pruhlednost:"
- Legend section: "Legenda barev"

<!-- From MainWindow.xaml right column — inline preview dock labels -->
Inline preview dock labels:
- Length adjustments: "Upravy delek:"
- Buttons: -50, -10, +10, +50
- Collisions: "Kolize"
- Normalize: "Sjednotit"
- Heatmap: "Heatmapa"
- Layers: "Vrstvy"
- Show all: "Zobrazit vse"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write preview.md — inline dock, standalone window, layers, heatmap, length adjustments</name>
  <files>docs/pruvodce/preview.md</files>
  <read_first>
    - docs/pruvodce/preview.md (current stub)
    - docs/pruvodce/parametry.md (check anchor slugs for cross-links)
    - .planning/phases/03-user-guide-content/03-RESEARCH.md (canonical terminology for RebarPreviewWindow, MainWindow right column dock, content guide for preview.md)
    - .planning/phases/03-user-guide-content/03-CONTEXT.md (locked decisions: Heatmapa goes on preview.md page)
    - ../WallReinf/WallReinf/Preview/RebarPreviewWindow.xaml (all preview window labels)
    - ../WallReinf/WallReinf/MainWindow.xaml (right column inline preview dock: Upravy delek, Kolize, Sjednotit, Heatmapa, Vrstvy)
  </read_first>
  <action>
Replace stub with full preview panel guide. Per CONTEXT.md, this page covers BOTH the inline dock in MainWindow right column AND the standalone RebarPreviewWindow, plus the Heatmapa feature.

**Page structure:**

```
# Preview panel

!!! info "Verze pluginu: [VERZE]"
    Tato stranka popisuje chovani pluginu verze [VERZE].

[One sentence: preview panel shows planned reinforcement before generation, available as inline dock and standalone window]

## Inline preview (hlavni okno)
```
- Right column of MainWindow shows live preview of planned reinforcement
- Updates automatically when parameters change
- Controls in the inline dock:
  - **Vrstvy** — toggle layer visibility (Zobrazit vse button)
  - **Heatmapa** — see heatmap section below
  - **Upravy delek:** — length adjustment strip with **-50**, **-10**, **+10**, **+50** buttons
  - **Kolize** button — check for collisions with edges and openings
  - **Sjednotit** button — normalize lengths of selected rows

Screenshot placeholder: MainWindow with inline preview visible in right column.

```
## Samostatne okno preview
```
- Standalone **Preview vyztuze** window (how to open it — check XAML for launch mechanism)
- Full-screen view of the same preview
- **Vrstvy vyztuze** panel on the right side
  - **Vse** / **Nic** buttons for show/hide all layers
- **Uprava delky (vybrane rady):** — same length adjustment controls
- **Zkontrolovat kolize** button
- **Sjednotit vybrane** button
- Statistics bar: **Celkem: X prutu, Y delek, Z varovani**
- **Generovat** and **Zrusit** buttons

!!! tip "Tip — Upravy v preview se zachovaji"
    Vsechny zmeny delek a jine upravy provedene v preview se zachovaji po kliknuti na **Generovat**. Neni treba je zadavat znovu.

Screenshot placeholder: Standalone preview window with layers visible.

```
## Vrstvy vyztuze
```
- Explain layer concept: each rebar type (vertical S1, vertical S2, horizontal S1, horizontal S2, edge, opening edge, stirrups, flags) is a separate layer
- Toggle visibility per layer
- Useful for reviewing one type at a time

```
## Heatmapa
```
Per CONTEXT.md: Heatmapa (O x delka matice) goes on this page.
- **Heatmapa (O x delka)** expander in preview
- Matrix showing count of bars for each combination of diameter and length
- Click cell to filter visible layers to that combination
- Useful for identifying dominant bar sizes and checking for unusual lengths

Screenshot placeholder: Heatmap matrix with some cells highlighted.

```
## Upravy delek
```
- Select rows in preview → use **-50**, **-10**, **+10**, **+50** to adjust lengths
- **Zkontrolovat kolize** after adjustments to verify no bars collide with edges or openings
- **Sjednotit** / **Sjednotit vybrane** to normalize selected rows to the same length

!!! warning "Pozor"
    Po uprave delek vzdy spustte **Zkontrolovat kolize** aby se overilo ze zkracene/prodlouzene pruty nekoliduji s okraji nebo otvory.

Cross-link: "Nastaveni prumeru a roztecu viz [Hlavni parametry](parametry.md)."

All Czech prose. Bold UI elements per XAML. Workflow-oriented.
  </action>
  <verify>
    <automated>cd "C:/Users/Evgenij Bogdanovič/Documents/WRW/WallReinf-wiki" && python -m mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/pruvodce/preview.md contains `!!! info "Verze pluginu: [VERZE]"`
    - docs/pruvodce/preview.md contains `**Vrstvy**` or `**Vrstvy výztuže**`
    - docs/pruvodce/preview.md contains `**Heatmapa**` or `Heatmapa`
    - docs/pruvodce/preview.md contains `**Kolize**` or `**Zkontrolovat kolize**`
    - docs/pruvodce/preview.md contains `**Sjednotit**` or `**Sjednotit vybrané**`
    - docs/pruvodce/preview.md contains `**Generovat**`
    - docs/pruvodce/preview.md contains `Úprav` (length adjustments section)
    - docs/pruvodce/preview.md contains `parametry.md` (cross-link)
    - docs/pruvodce/preview.md contains at least 2 screenshot placeholders `![`
    - docs/pruvodce/preview.md contains at least one `!!! tip` or `!!! warning`
    - docs/pruvodce/preview.md has more than 80 lines
    - `python -m mkdocs build --strict` exits 0
  </acceptance_criteria>
  <done>preview.md covers both inline dock and standalone preview window, layer controls, heatmap matrix, length adjustments, and collision checking. Cross-links to parametry.md. Version badge, admonitions, screenshot placeholders present. mkdocs build --strict passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write validace.md and pdf-export.md — validation window and export</name>
  <files>docs/pruvodce/validace.md, docs/pruvodce/pdf-export.md</files>
  <read_first>
    - docs/pruvodce/validace.md (current stub)
    - docs/pruvodce/pdf-export.md (current stub)
    - docs/pruvodce/preview.md (already filled — check how preview is explained to avoid confusion between preview and validation)
    - .planning/phases/03-user-guide-content/03-RESEARCH.md (canonical terminology for RebarValidationWindow, content guide for validace.md and pdf-export.md)
    - .planning/phases/03-user-guide-content/03-CONTEXT.md (locked decisions: writing style, admonition placement)
    - ../WallReinf/WallReinf/Validation/RebarValidationWindow.xaml (all validation window labels, layer legend entries, export buttons)
  </read_first>
  <action>
**Part A: Write docs/pruvodce/validace.md (GUIDE-06)**

Replace stub. The validation window shows ALREADY GENERATED rebar from the Tekla model (reality mode), unlike preview which shows PLANNED rebar.

Page structure:

```
# Validacni okno

!!! info "Verze pluginu: [VERZE]"
    Tato stranka popisuje chovani pluginu verze [VERZE].

[One sentence: validation window shows actual generated reinforcement from the Tekla model for visual checking]

## Spusteni validace
```
- How to open the validation window (check XAML for trigger)
- Window title: **Graficka validace vyztuze**

!!! note "Poznamka — Validace vs. Preview"
    Validacni okno zobrazuje **skutecnou vyztuz z Tekla modelu** (jiz vygenerovanou), nikoliv planovany navrh jako preview panel. Nejprve je nutne vyztuz vygenerovat pres **Generovat**.

Cross-link: "Preview planované výztuže viz [Preview panel](preview.md)."

```
## Skutecna vyztuz z Tekla modelu
```
- **Skutecna vyztuz z Tekla modelu** label — reality mode indicator
- **Obnovit** button — reloads data from Tekla model
- When to use: after generation, or after manual edits in Tekla

Screenshot placeholder: Validation window showing generated rebar.

```
## Vrstvy a legenda
```
- **Vrstvy vyztuze** panel — same concept as preview but showing real rebar
- Layer legend entries with their Tekla classes:
  - Svisla (Class 3)
  - Vodorovna (Class 6)
  - Lemovani hrany (Class 7, 8)
  - Lemovani otvoru (Class 9)
  - Prapory (Class 9 - Flag)
  - Trminky (Class 11)
- **Pruhlednost:** slider — adjust transparency of layers
- **Legenda barev** collapsible expander — color key for each layer type

Screenshot placeholder: Validation window with legend expanded and multiple layers visible.

```
## Interpretace vysledku
```
- How to read the validation view
- Check for missing rebar, gaps, collisions
- Compare with design intent
- Use layer visibility to isolate specific rebar types

---

**Part B: Write docs/pruvodce/pdf-export.md (GUIDE-07)**

Replace stub. Export happens FROM the validation window.

Page structure:

```
# PDF export

!!! info "Verze pluginu: [VERZE]"
    Tato stranka popisuje chovani pluginu verze [VERZE].

[One sentence: export graphical validation view as PDF, PNG, or print]

## Vytvoreni exportu
```
Numbered steps:
1. Open validation window (cross-link: "[Validacni okno](validace.md)")
2. Set desired layer visibility — only visible layers are exported
3. Choose export action:
   - **Export PDF** — saves to PDF file
   - **Export PNG** — saves to PNG image
   - **Tisk** — sends to printer

!!! tip "Tip — Pred exportem upravte vrstvy"
    Vypnete vrstvy ktere v exportu nepotrebujete. Export zachyti presne to co vidite ve validacnim okne — vcetne zapnutych/vypnutych vrstev a nastavene pruhlednosti.

Screenshot placeholder: Validation window with Export buttons highlighted.

```
## Obsah exportu
```
- What the export contains: graphical view of the wall with visible reinforcement layers
- Color coding matches the layer legend
- Scale and dimensions depend on wall size
- Version and metadata included

Screenshot placeholder: Example PDF output (or placeholder for Phase 5).

Cross-link: "Podrobnosti o vrstvach a legende viz [Validacni okno](validace.md#vrstvy-a-legenda)."

All Czech prose. Bold UI elements. XAML terminology. Workflow-oriented.
  </action>
  <verify>
    <automated>cd "C:/Users/Evgenij Bogdanovič/Documents/WRW/WallReinf-wiki" && python -m mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/pruvodce/validace.md contains `!!! info "Verze pluginu: [VERZE]"`
    - docs/pruvodce/validace.md contains `**Grafická validace výztuže**`
    - docs/pruvodce/validace.md contains `**Skutečná výztuž z Tekla modelu**` or `Skutečná výztuž`
    - docs/pruvodce/validace.md contains `**Obnovit**`
    - docs/pruvodce/validace.md contains `Class 3` or `Class 6` or `Class 11` (layer class references)
    - docs/pruvodce/validace.md contains `**Průhlednost**`
    - docs/pruvodce/validace.md contains `**Legenda barev**`
    - docs/pruvodce/validace.md contains `preview.md` (cross-link distinguishing validation from preview)
    - docs/pruvodce/validace.md contains at least one `!!! note` explaining validation vs preview
    - docs/pruvodce/validace.md contains at least one screenshot placeholder `![`
    - docs/pruvodce/validace.md has more than 60 lines
    - docs/pruvodce/pdf-export.md contains `!!! info "Verze pluginu: [VERZE]"`
    - docs/pruvodce/pdf-export.md contains `**Export PDF**`
    - docs/pruvodce/pdf-export.md contains `**Export PNG**`
    - docs/pruvodce/pdf-export.md contains `**Tisk**`
    - docs/pruvodce/pdf-export.md contains `validace.md` (cross-link)
    - docs/pruvodce/pdf-export.md contains at least one `!!! tip`
    - docs/pruvodce/pdf-export.md contains at least one screenshot placeholder `![`
    - docs/pruvodce/pdf-export.md has more than 40 lines
    - `python -m mkdocs build --strict` exits 0
  </acceptance_criteria>
  <done>validace.md covers the validation window with reality mode explanation, layer legend with class numbers, transparency, and how to interpret results. pdf-export.md covers PDF/PNG/Print export from validation window with layer visibility tip. Both cross-link appropriately. Version badges, admonitions, screenshot placeholders present. mkdocs build --strict passes.</done>
</task>

</tasks>

<verification>
- `python -m mkdocs build --strict` exits 0
- `grep -c "Verze pluginu" docs/pruvodce/preview.md docs/pruvodce/validace.md docs/pruvodce/pdf-export.md` shows >= 1 each
- `grep "validace.md" docs/pruvodce/pdf-export.md` shows cross-link exists
- `grep "preview.md" docs/pruvodce/validace.md` shows cross-link exists
- All 3 files have substantive content (more than 40 lines each)
</verification>

<success_criteria>
- preview.md covers both inline dock and standalone preview, layers, heatmap, length adjustments
- validace.md covers reality mode, layer legend with class numbers, transparency, interpretation
- pdf-export.md covers all 3 export actions with workflow steps and layer visibility tip
- All pages distinguish between preview (planned) and validation (actual) clearly
- Cross-links connect the three pages logically
- All pages have version badges, genuine admonitions, screenshot placeholders
- mkdocs build --strict exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-guide-content/03-04-SUMMARY.md`
</output>
