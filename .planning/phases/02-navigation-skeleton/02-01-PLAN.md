---
phase: 02-navigation-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - docs/pruvodce/index.md
  - docs/pruvodce/pozadavky.md
  - docs/pruvodce/instalace.md
  - docs/pruvodce/pripojeni.md
  - docs/pruvodce/zakladni-workflow.md
  - docs/pruvodce/parametry.md
  - docs/pruvodce/otvory.md
  - docs/pruvodce/trminky.md
  - docs/pruvodce/diagonaly.md
  - docs/pruvodce/preview.md
  - docs/pruvodce/validace.md
  - docs/pruvodce/pdf-export.md
  - docs/reference/index.md
  - docs/reference/parametry.md
  - docs/faq.md
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06

must_haves:
  truths:
    - "mkdocs.yml has an explicit nav: key listing every page (no auto-discovery)"
    - "Sidebar shows Pruvodce and Reference sections with all sub-pages"
    - "FAQ appears as a standalone tab in navigation"
    - "Every stub page renders with H2-based table of contents"
    - "Breadcrumbs are visible on subpages (pruvodce/*, reference/*)"
    - "All filenames are ASCII-only (no Czech diacritics in paths)"
    - "mkdocs build --strict passes with zero warnings"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Explicit nav tree with 4 top-level entries"
      contains: "nav:"
    - path: "docs/pruvodce/index.md"
      provides: "Pruvodce section index"
      contains: "# Pruvodce"
    - path: "docs/pruvodce/pozadavky.md"
      provides: "Requirements stub page"
      contains: "# Pozadavky"
    - path: "docs/pruvodce/instalace.md"
      provides: "Installation stub page"
      contains: "# Instalace"
    - path: "docs/pruvodce/pripojeni.md"
      provides: "Connection stub page"
    - path: "docs/pruvodce/zakladni-workflow.md"
      provides: "Workflow stub page"
    - path: "docs/pruvodce/parametry.md"
      provides: "Parameters guide stub"
    - path: "docs/pruvodce/otvory.md"
      provides: "Openings stub"
    - path: "docs/pruvodce/trminky.md"
      provides: "Stirrups stub"
    - path: "docs/pruvodce/diagonaly.md"
      provides: "Diagonal bars stub"
    - path: "docs/pruvodce/preview.md"
      provides: "Preview panel stub"
    - path: "docs/pruvodce/validace.md"
      provides: "Validation window stub"
    - path: "docs/pruvodce/pdf-export.md"
      provides: "PDF export stub"
    - path: "docs/reference/index.md"
      provides: "Reference section index"
    - path: "docs/reference/parametry.md"
      provides: "Parameter reference stub"
    - path: "docs/faq.md"
      provides: "FAQ stub page"
  key_links:
    - from: "mkdocs.yml nav:"
      to: "docs/pruvodce/*.md"
      via: "explicit nav entries"
      pattern: "pruvodce/"
    - from: "mkdocs.yml nav:"
      to: "docs/reference/*.md"
      via: "explicit nav entries"
      pattern: "reference/"
    - from: "mkdocs.yml nav:"
      to: "docs/faq.md"
      via: "explicit nav entry"
      pattern: "faq.md"
---

<objective>
Create the complete navigation skeleton for the WallReinf Wiki: expand the mkdocs.yml nav configuration from a single entry to a full three-tab tree (Pruvodce, Reference, FAQ) and create all 15 stub files with Czech headings and placeholder sections.

Purpose: Lock the information architecture (file names, directory structure, nav order) before any content investment. Every subsequent phase builds on this stable structure.
Output: 15 new stub Markdown files, updated mkdocs.yml with full nav tree, passing mkdocs build --strict.
</objective>

<execution_context>
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Evgenij Bogdanovič/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-skeleton/02-CONTEXT.md
@.planning/phases/02-navigation-skeleton/02-RESEARCH.md

<interfaces>
<!-- Current mkdocs.yml nav (to be replaced): -->
nav:
  - Domů: index.md

<!-- Current docs/index.md (DO NOT modify — leave bold text references as-is): -->
- **Začínáme** — instalace, požadavky a první spuštění pluginu
- **Průvodce funkcemi** — nastavení parametrů, otvory, třmínky, preview a validace
- **Reference** — kompletní přehled všech parametrů pluginu
- **FAQ** — časté dotazy a řešení problémů

<!-- Theme features already enabled (no changes needed): -->
features:
  - navigation.tabs
  - navigation.sections
  - navigation.top
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nav config and all stub files</name>
  <files>mkdocs.yml, docs/pruvodce/index.md, docs/pruvodce/pozadavky.md, docs/pruvodce/instalace.md, docs/pruvodce/pripojeni.md, docs/pruvodce/zakladni-workflow.md, docs/pruvodce/parametry.md, docs/pruvodce/otvory.md, docs/pruvodce/trminky.md, docs/pruvodce/diagonaly.md, docs/pruvodce/preview.md, docs/pruvodce/validace.md, docs/pruvodce/pdf-export.md, docs/reference/index.md, docs/reference/parametry.md, docs/faq.md</files>
  <read_first>mkdocs.yml, docs/index.md, .planning/phases/02-navigation-skeleton/02-CONTEXT.md, .planning/phases/02-navigation-skeleton/02-RESEARCH.md</read_first>
  <action>
**Step 1: Update mkdocs.yml nav section.**

Replace the existing `nav:` block (currently just `- Domů: index.md`) with the complete tree. Keep ALL other config (site_name, site_url, theme, plugins, markdown_extensions) unchanged. The new nav block:

```yaml
nav:
  - Domů: index.md
  - Průvodce:
    - pruvodce/index.md
    - Požadavky: pruvodce/pozadavky.md
    - Instalace: pruvodce/instalace.md
    - Připojení k modelu: pruvodce/pripojeni.md
    - Základní workflow: pruvodce/zakladni-workflow.md
    - Hlavní parametry: pruvodce/parametry.md
    - Otvory: pruvodce/otvory.md
    - Třmínky: pruvodce/trminky.md
    - Diagonální pruty: pruvodce/diagonaly.md
    - Preview panel: pruvodce/preview.md
    - Validační okno: pruvodce/validace.md
    - PDF export: pruvodce/pdf-export.md
  - Reference:
    - reference/index.md
    - Parametry: reference/parametry.md
  - FAQ: faq.md
```

IMPORTANT: Section index pages (`pruvodce/index.md`, `reference/index.md`) are listed WITHOUT a label — MkDocs Material infers the label from the H1 heading.

**Step 2: Create directories.**

Create `docs/pruvodce/` and `docs/reference/` directories.

**Step 3: Create all 15 stub files.**

Each stub follows this pattern: Czech H1 heading, 1-2 sentence Czech description, H2 section placeholders. NO Markdown cross-links (use bold text only to avoid --strict failures). Each stub must have at least 2 H2 headings to generate TOC entries (satisfies NAV-04).

Create these files with EXACT content:

**docs/pruvodce/index.md:**
```markdown
# Průvodce

Průvodce vás provede celým procesem práce s pluginem Wall Reinforcement Wizard — od instalace přes nastavení parametrů až po generování a validaci výztuže.

## Začínáme

V sekci **Začínáme** najdete informace o požadavcích, instalaci, připojení k modelu a základním workflow.

## Průvodce funkcemi

Jednotlivé stránky popisují práci s hlavními parametry, otvory, třmínky, diagonálními pruty, preview panelem, validačním oknem a PDF exportem.
```

**docs/pruvodce/pozadavky.md:**
```markdown
# Požadavky

Tato stránka popisuje systémové požadavky pro spuštění pluginu Wall Reinforcement Wizard.

## Tekla Structures

## Operační systém

## .NET Framework
```

**docs/pruvodce/instalace.md:**
```markdown
# Instalace a spuštění

Tato stránka popisuje, jak nainstalovat a poprvé spustit plugin Wall Reinforcement Wizard v Tekla Structures.

## Instalace pluginu

## Spuštění pluginu
```

**docs/pruvodce/pripojeni.md:**
```markdown
# Připojení k modelu

Tato stránka popisuje, jak plugin propojit s otevřeným modelem v Tekla Structures a jak vybrat stěnu pro vyztužení.

## Otevření modelu

## Výběr stěny
```

**docs/pruvodce/zakladni-workflow.md:**
```markdown
# Základní workflow

Tato stránka popisuje základní pracovní postup — od výběru stěny přes nastavení parametrů až po generování výztuže.

## Výběr stěny

## Nastavení parametrů

## Generování výztuže
```

**docs/pruvodce/parametry.md:**
```markdown
# Hlavní parametry

Tato stránka popisuje nastavení hlavních parametrů výztuže — průměry prutů, krytí, rozteče a okrajové pruty.

## Průměry prutů

## Krytí

## Rozteče

## Okrajové pruty
```

**docs/pruvodce/otvory.md:**
```markdown
# Otvory

Tato stránka popisuje práci s otvory ve stěně — jejich detekci, nastavení a generování U-prutů kolem otvorů.

## Detekce otvorů

## Nastavení otvorů

## U-pruty kolem otvorů
```

**docs/pruvodce/trminky.md:**
```markdown
# Třmínky a interakce segmentů

Tato stránka popisuje nastavení třmínků a způsob interakce mezi segmenty výztuže.

## Nastavení třmínků

## Interakce segmentů
```

**docs/pruvodce/diagonaly.md:**
```markdown
# Diagonální pruty

Tato stránka popisuje nastavení a generování diagonálních prutů ve výztuži stěny.

## Nastavení diagonálních prutů

## Umístění diagonál
```

**docs/pruvodce/preview.md:**
```markdown
# Preview panel

Tato stránka popisuje práci s preview panelem, který zobrazuje vizualizaci plánované výztuže před jejím vygenerováním.

## Zobrazení preview

## Interpretace vizualizace
```

**docs/pruvodce/validace.md:**
```markdown
# Validační okno

Tato stránka popisuje validační okno, které kontroluje správnost nastavení parametrů před generováním výztuže.

## Spuštění validace

## Interpretace výsledků
```

**docs/pruvodce/pdf-export.md:**
```markdown
# PDF export

Tato stránka popisuje export výsledků a nastavení do PDF formátu.

## Vytvoření exportu

## Obsah PDF dokumentu
```

**docs/reference/index.md:**
```markdown
# Reference

Referenční část obsahuje kompletní přehled všech parametrů pluginu Wall Reinforcement Wizard s jejich výchozími hodnotami a platnými rozsahy.

## Obsah reference

V sekci **Parametry** najdete kompletní tabulku všech parametrů seskupenou podle sekcí pluginu.
```

**docs/reference/parametry.md:**
```markdown
# Parametry

Kompletní přehled všech parametrů pluginu Wall Reinforcement Wizard seskupený podle sekcí UI.

## Obecné parametry

## Parametry výztuže

## Parametry otvorů
```

**docs/faq.md:**
```markdown
# FAQ

Časté dotazy a řešení známých problémů při práci s pluginem Wall Reinforcement Wizard.

## Připojení k Tekla Structures

## Generování výztuže

## Validace
```

**Step 4: Do NOT modify docs/index.md.** Leave its bold text references as-is (per Phase 1 decision and research pitfall #5).

**Step 5: Verify the build.**

Run `mkdocs build --strict` from the repo root. It must exit 0 with zero warnings. If it fails, fix the issue (most likely a nav path typo or missing file).

Also verify ASCII-only filenames: run `find docs/ -name "*.md" | grep -P "[^\x00-\x7F]"` — must produce zero output.
  </action>
  <verify>
    <automated>mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "nav:" mkdocs.yml` returns 1 (explicit nav present — NAV-01)
- `grep -c "pruvodce/" mkdocs.yml` returns at least 12 (all guide pages in nav)
- `grep -c "reference/" mkdocs.yml` returns at least 2 (reference section in nav)
- `grep "faq.md" mkdocs.yml` returns a match (FAQ in nav)
- `ls docs/pruvodce/*.md | wc -l` returns 12 (index + 11 guide stubs)
- `ls docs/reference/*.md | wc -l` returns 2 (index + parametry)
- `test -f docs/faq.md` succeeds (FAQ stub exists)
- `find docs/ -name "*.md" | grep -P "[^\x00-\x7F]" | wc -l` returns 0 (NAV-06: ASCII-only filenames)
- `mkdocs build --strict` exits with code 0 and zero warnings
- Each stub file contains at least one H1 (`# `) and at least two H2 (`## `) headings (for NAV-04 TOC)
  </acceptance_criteria>
  <done>All 15 stub files created with Czech headings and H2 placeholders. mkdocs.yml nav tree lists all pages explicitly. mkdocs build --strict passes with zero warnings. All filenames are ASCII-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify navigation renders correctly in browser</name>
  <files>none (verification only)</files>
  <action>Run `mkdocs serve` to start the local development server. Then present the verification steps to the user for visual confirmation of tabs, sidebar, breadcrumbs, and TOC rendering.</action>
  <what-built>Complete navigation skeleton with three-tab layout (Pruvodce, Reference, FAQ), 15 stub pages with Czech headings and section placeholders, and explicit nav configuration in mkdocs.yml.</what-built>
  <how-to-verify>
1. Run `mkdocs serve` in terminal (or visit the deployed site if pushed)
2. Verify the header shows tabs: **Domu**, **Pruvodce**, **Reference**, **FAQ**
3. Click **Pruvodce** tab — sidebar should show: Pruvodce (overview), Pozadavky, Instalace, Pripojeni k modelu, Zakladni workflow, Hlavni parametry, Otvory, Trminky, Diagonalni pruty, Preview panel, Validacni okno, PDF export
4. Click **Reference** tab — sidebar should show: Reference (overview), Parametry
5. Click **FAQ** tab — FAQ page loads
6. On any subpage (e.g., pruvodce/parametry), verify:
   - Breadcrumbs are visible above the content (NAV-05)
   - Right-side table of contents shows H2 headings (NAV-04)
7. Click through ALL sidebar links — every page loads without 404 errors (NAV-03)
8. Check browser URL bar on 3+ pages — no percent-encoded Czech characters in paths (NAV-06)
  </how-to-verify>
  <verify>Visual inspection by user — all 8 verification steps pass</verify>
  <done>User confirms tabs, sidebar, breadcrumbs, TOC, and URL paths all render correctly. No 404 errors on any page.</done>
  <resume-signal>Type "approved" if navigation looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `mkdocs build --strict` passes with zero warnings (validates NAV-01 nav completeness, file existence, no orphans)
- `find docs/ -name "*.md" | grep -P "[^\x00-\x7F]"` returns empty (NAV-06 ASCII filenames)
- Visual confirmation that tabs, sidebar, breadcrumbs, and TOC render correctly (NAV-02, NAV-03, NAV-04, NAV-05)
</verification>

<success_criteria>
- mkdocs.yml has explicit nav: with all 16 entries (home + 12 guide + 2 reference + FAQ)
- All 15 new stub files exist with Czech H1 headings and H2 section placeholders
- mkdocs build --strict exits 0 with zero warnings
- Browser shows three-tab navigation with full sidebar in each section
- Breadcrumbs and TOC visible on subpages
- No Czech diacritics in any filename
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-skeleton/02-01-SUMMARY.md`
</output>
